import React from 'react';

const About = ({reference}) => {
    return (
        <div ref={reference} className="whiteBackground">
            //react transition on init section <br/>
            //add floating html and coding symbols: [text], //text
        </div>
    );
};

export default About;